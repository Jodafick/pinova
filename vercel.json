{
  "installCommand": "pnpm --dir ../pinova-shared install --frozen-lockfile && pnpm --dir ../pinova-shared build && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm build:vercel",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
